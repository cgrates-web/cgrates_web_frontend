<main class="page-content">
  <div class="container xl">
    <div class="actions-subtitle">
      <h2>
        TpAliases list
      </h2>
      <ul class="actions">
        <TpIndexButtons
          @model={{this.model}}
          @tariffPlanId={{this.tariffPlanId}}
          @permittedFilters={{permittedFilters}}
          @refresh="refresh"
          @controller={{this}}
        />
      </ul>
    </div>
    <TotalRecords @totalRecords={{meta.total_records}} />
    <div class="row">
      <div class="col-md-9">
        <div class="table-responsive">
          <table class="table table-striped table-sm">
            <thead>
              <tr>
                <SortHeader
                  @label="ID"
                  @key="id"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Tenant"
                  @key="tenant"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                  @data-test-sort-tenant={{true}}
                />
                <SortHeader
                  @label="Direction"
                  @key="direction"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Category"
                  @key="category"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Account"
                  @key="account"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Subject"
                  @key="subject"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Destination ID"
                  @key="destinationId"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Context"
                  @key="context"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Target"
                  @key="target"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Original"
                  @key="original"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Alias"
                  @key="alias"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Weight"
                  @key="weight"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <SortHeader
                  @label="Created at"
                  @key="createdAt"
                  @sortColumn={{sortColumn}}
                  @sortOrder={{sortOrder}}
                  @sortAction="sortBy"
                />
                <td>
                  Actions
                </td>
              </tr>
            </thead>
            <tbody>
              {{#each model as |tpAlias|}}
                <tr>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.id}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.tenant}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.direction}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.category}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.account}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.subject}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.destinationId}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.context}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.target}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.original}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.alias}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{tpAlias.weight}}
                    </LinkTo>
                  </td>
                  <td>
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias"
                      @model={{tpAlias}}
                    >
                      {{moment-format tpAlias.createdAt}}
                    </LinkTo>
                  </td>
                  <td class="actions">
                    <LinkTo
                      @route="tariff-plan.tp-aliases.tp-alias.edit"
                      @model={{tpAlias}}
                    >
                      <i
                        class="material-icons circle btn btn-primary"
                        data-test-tp-alias-edit
                      >
                        edit
                      </i>
                    </LinkTo>
                    <a class="ml-1" role="button" {{action "remove" tpAlias}}>
                      <i
                        class="material-icons circle btn btn-danger"
                        data-test-tp-alias-remove
                      >
                        remove
                      </i>
                    </a>
                  </td>
                </tr>
              {{/each}}
            </tbody>
          </table>
        </div>
        <PageNumbers
          @content={{pagination}}
          @action="toPage"
          @showFL={{true}}
          @numPagesToShow={{4}}
        />
      </div>
      <div class="col-md-3">
        <FiltersBlock @search={{action "search"}} as |filtersBlock|>
          <FilterText
            @label="Tenant"
            @key="tenant"
            @value={{tenant}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-tenant={{true}}
          />
          <FilterText
            @label="Direction"
            @key="direction"
            @value={{direction}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-direction={{true}}
          />
          <FilterText
            @label="Account"
            @key="account"
            @value={{account}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-account={{true}}
          />
          <FilterText
            @label="Destination ID"
            @key="destinationId"
            @value={{destinationId}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-destination-id={{true}}
          />
          <FilterText
            @label="Category"
            @key="category"
            @value={{category}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-category={{true}}
          />
          <FilterText
            @label="Subject"
            @key="subject"
            @value={{subject}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-subject={{true}}
          />
          <FilterText
            @label="Context"
            @key="context"
            @value={{context}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-context={{true}}
          />
          <FilterText
            @label="Target"
            @key="targetParam"
            @value={{targetParam}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-target={{true}}
          />
          <FilterText
            @label="Original"
            @key="original"
            @value={{original}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-original={{true}}
          />
          <FilterText
            @label="Alias"
            @key="alias"
            @value={{alias}}
            @onValueChange={{filtersBlock.pushValue}}
            @data-test-filter-alias={{true}}
          />
          <FilterNumber
            @label="Weight"
            @key="weight"
            @value={{weight}}
            @onValueChange={{filtersBlock.pushValue}}
            @step="0.1"
            @min="0"
            @data-test-filter-weight={{true}}
          />
        </FiltersBlock>
      </div>
    </div>
  </div>
</main>